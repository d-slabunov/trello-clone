const initialState = {
  confirmed: true,
  // token: 'testtoken',
};

const userReducer = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};

export default userReducer;
